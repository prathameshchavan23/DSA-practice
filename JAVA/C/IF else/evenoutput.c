#include<stdio.h>
int main(){
    int n;
    printf("Enter a number");
    scanf("%d",&n);
if (n%2==0)//comprator operator (==)
{
    printf("Even no");
} 
else{
    printf("odd no");
}
    return 0;
}